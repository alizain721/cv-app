<%= form_with(model: cv, local: true) do |form| %>
  <% if cv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cv.errors.count, "error") %> prohibited this cv from being saved:</h2>

      <ul>
      <% cv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :education %>
    <%= form.cktext_area :education, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :experience %>
    <%= form.cktext_area :experience, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :skills %>
    <%= form.text_field :skills, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
